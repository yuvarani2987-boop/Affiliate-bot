services:
  - type: web
    name: affiliate-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app_v2:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
